const NOT_SYMBOL = '!';

const AND_SYMBOL = '&&';

const OR_SYMBOL = '||';

const RESERVED_COMMANDS = [
    '--tags'
];

module.exports = {
    AND_SYMBOL,
    OR_SYMBOL,
    NOT_SYMBOL,
    RESERVED_COMMANDS,
};

